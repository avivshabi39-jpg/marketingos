"use client";

export default function IntakeError() {
  return (
    <div dir="rtl" style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", fontFamily: "Arial, sans-serif" }}>
      <p style={{ fontSize: "16px", color: "#6b7280" }}>שגיאה בטעינת הטופס. אנא נסה שוב.</p>
    </div>
  );
}
